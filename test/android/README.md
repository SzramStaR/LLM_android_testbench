# MLC App

This app is for testing LLM inference metrics using the MLC LLM framework.

## Configuration

- Models are configured via `MLCChat/mlc-package-config.json`.
- API configuration has to be changed in `MLCChat/app/src/main/java/ai/mlc/mlcchat/AppViewModel.kt`.
